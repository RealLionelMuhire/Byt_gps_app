# System Architecture - Clerk Authentication Flow

## 📊 System Overview

```
┌─────────────────┐
│  Mobile App     │
│  (React Native) │
│  + Clerk Auth   │
└────────┬────────┘
         │
         │ 1. User Signs In
         ├──────────────────────────────────┐
         │                                  │
         ▼                                  ▼
┌────────────────┐                ┌─────────────────┐
│  Clerk Service │                │  GPS Backend    │
│                │                │  (FastAPI)      │
└────────┬───────┘                └────────┬────────┘
         │                                  │
         │ 2. Returns User Data             │
         │    (clerk_user_id, email, name)  │
         │                                  │
         └──────────────────────────────────┤
                                           │
         ┌──────────────────────────────────┘
         │
         │ 3. POST /api/auth/sync
         │    Sync user to backend
         │
         ▼
┌────────────────────────────────────────────┐
│           Backend Database                  │
│              (PostgreSQL)                   │
│                                            │
│  ┌──────────┐      ┌──────────┐           │
│  │  users   │      │ devices  │           │
│  │          │      │          │           │
│  │  id  ────┼──────┤ user_id  │           │
│  │  clerk_  │      │  imei    │           │
│  │  user_id │      │  name    │           │
│  │  email   │      │  status  │           │
│  │  name    │      └────┬─────┘           │
│  └──────────┘           │                 │
│                         │                 │
│                    ┌────▼──────┐          │
│                    │ locations │          │
│                    │           │          │
│                    │ device_id │          │
│                    │ latitude  │          │
│                    │ longitude │          │
│                    │ timestamp │          │
│                    └───────────┘          │
└────────────────────────────────────────────┘
```

---

## 🔄 Authentication Flow

```
┌──────────────┐
│ Mobile App   │
│ User Opens   │
└──────┬───────┘
       │
       │ Step 1: Sign In with Clerk
       ▼
┌──────────────┐
│ Clerk Auth   │◄─── Email/Google/Apple Sign-In
└──────┬───────┘
       │
       │ Returns: { id, email, name }
       ▼
┌──────────────┐
│ Mobile App   │
│ Receives     │
│ Clerk Data   │
└──────┬───────┘
       │
       │ Step 2: Sync to Backend
       │ POST /api/auth/sync
       │ {
       │   clerk_user_id: "user_xxx",
       │   email: "user@example.com",
       │   name: "John Doe"
       │ }
       ▼
┌──────────────┐
│ Backend      │
│ Creates/     │◄─── Upsert Operation
│ Updates User │      (Create if new, Update if exists)
└──────┬───────┘
       │
       │ Returns: User Object with ID
       ▼
┌──────────────┐
│ Mobile App   │
│ Stores       │
│ clerk_user_id│◄─── Store in AsyncStorage
└──────┬───────┘
       │
       │ Step 3: Make API Requests
       │ Add Header: X-Clerk-User-Id
       ▼
┌──────────────────────────────────┐
│ All Subsequent API Calls         │
│                                  │
│ GET /api/devices                 │
│ GET /api/locations/:id/latest    │
│ GET /api/locations/:id/history   │
│                                  │
│ Header: X-Clerk-User-Id: user_xxx│
└──────────────────────────────────┘
```

---

## 🔐 Access Control Flow

```
┌─────────────────┐
│ API Request     │
│ with Header     │
│ X-Clerk-User-Id │
└────────┬────────┘
         │
         ▼
    ┌────────────────┐
    │ Extract Header │
    └────────┬───────┘
             │
             ▼
    ┌─────────────────┐
    │ Lookup User in  │
    │ Database by     │
    │ clerk_user_id   │
    └────────┬────────┘
             │
        ┌────▼────┐
        │ Found?  │
        └────┬────┘
             │
      ┌──────┴──────┐
      │             │
     Yes           No
      │             │
      ▼             ▼
┌──────────┐  ┌──────────┐
│ Filter   │  │ Return   │
│ Data by  │  │ Empty or │
│ user_id  │  │ 403      │
└────┬─────┘  └──────────┘
     │
     ▼
┌──────────┐
│ Return   │
│ User's   │
│ Data     │
└──────────┘
```

---

## 📡 API Request Flow

### Example: Get Devices

```
Mobile App Request:
┌───────────────────────────────────────────┐
│ GET /api/devices                          │
│ Headers:                                  │
│   X-Clerk-User-Id: user_2abc123xyz        │
└──────────────────┬────────────────────────┘
                   │
                   ▼
            Backend Processing:
┌─────────────────────────────────────────────┐
│ 1. Extract: user_id = "user_2abc123xyz"    │
│ 2. Query DB: SELECT * FROM users           │
│              WHERE clerk_user_id = ?        │
│ 3. Found: user.id = 42                     │
│ 4. Query: SELECT * FROM devices            │
│           WHERE user_id = 42               │
│ 5. Return: List of user's devices          │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
            Response:
┌───────────────────────────────────────────┐
│ [                                         │
│   {                                       │
│     "id": 1,                              │
│     "imei": "123456789",                  │
│     "name": "My GPS Tracker",             │
│     "user_id": 42,                        │
│     "status": "online",                   │
│     ...                                   │
│   }                                       │
│ ]                                         │
└───────────────────────────────────────────┘
```

---

## 🗄️ Database Schema

```
┌─────────────────────────────────────────────┐
│              users                          │
├──────────────┬─────────────┬────────────────┤
│ id           │ INTEGER     │ PRIMARY KEY    │
│ clerk_user_id│ VARCHAR(255)│ UNIQUE NOT NULL│
│ email        │ VARCHAR(255)│ NOT NULL       │
│ name         │ VARCHAR(255)│                │
│ created_at   │ TIMESTAMP   │                │
│ updated_at   │ TIMESTAMP   │                │
└──────────────┴─────────────┴────────────────┘
                     │
                     │ one-to-many
                     ▼
┌─────────────────────────────────────────────┐
│             devices                         │
├──────────────┬─────────────┬────────────────┤
│ id           │ INTEGER     │ PRIMARY KEY    │
│ user_id      │ INTEGER     │ FK → users(id) │
│ imei         │ VARCHAR(20) │ UNIQUE NOT NULL│
│ name         │ VARCHAR(100)│ NOT NULL       │
│ status       │ VARCHAR(20) │                │
│ last_latitude│ FLOAT       │                │
│ last_longitude│FLOAT       │                │
│ ...          │             │                │
└──────────────┴─────────────┴────────────────┘
                     │
                     │ one-to-many
                     ▼
┌─────────────────────────────────────────────┐
│            locations                        │
├──────────────┬─────────────┬────────────────┤
│ id           │ INTEGER     │ PRIMARY KEY    │
│ device_id    │ INTEGER     │ FK → devices   │
│ latitude     │ FLOAT       │                │
│ longitude    │ FLOAT       │                │
│ speed        │ FLOAT       │                │
│ timestamp    │ TIMESTAMP   │                │
│ ...          │             │                │
└──────────────┴─────────────┴────────────────┘
```

---

## 🔄 User Sync Operation (Upsert)

```
POST /api/auth/sync
{
  "clerk_user_id": "user_abc",
  "email": "user@example.com",
  "name": "John Doe"
}
        │
        ▼
┌────────────────────────────────┐
│ Check if user exists           │
│ SELECT * FROM users            │
│ WHERE clerk_user_id = ?        │
└────────┬───────────────────────┘
         │
    ┌────▼────┐
    │ Exists? │
    └────┬────┘
         │
    ┌────┴────┐
    │         │
   Yes       No
    │         │
    ▼         ▼
┌─────────┐ ┌─────────┐
│ UPDATE  │ │ INSERT  │
│ email   │ │ New     │
│ name    │ │ User    │
│ updated_│ │ Record  │
│   at    │ │         │
└────┬────┘ └────┬────┘
     │           │
     └─────┬─────┘
           │
           ▼
    ┌──────────┐
    │ COMMIT   │
    └────┬─────┘
         │
         ▼
    ┌──────────┐
    │ Return   │
    │ User     │
    │ Object   │
    └──────────┘
```

---

## 🎯 Endpoint Relationships

```
Authentication:
┌────────────────────────────────────┐
│ POST /api/auth/sync                │ ──► Create/Update User
│ GET  /api/auth/user/{clerk_user_id}│ ──► Get User Info
└────────────────────────────────────┘
                │
                │ Provides user_id
                ▼
Device Management:
┌────────────────────────────────────┐
│ GET  /api/devices                  │ ──► List User's Devices
│ POST /api/devices                  │ ──► Create Device for User
│ POST /api/devices/{id}/assign      │ ──► Assign Device to User
└────────────────────────────────────┘
                │
                │ Requires device ownership
                ▼
Location Tracking:
┌────────────────────────────────────┐
│ GET  /api/locations/{id}/latest    │ ──► Latest Location
│ GET  /api/locations/{id}/history   │ ──► Location History
│ GET  /api/locations/{id}/route     │ ──► Device Route
└────────────────────────────────────┘
```

---

## 📱 Mobile App State Management

```
App Lifecycle:

┌────────────┐
│ App Start  │
└─────┬──────┘
      │
      ▼
┌─────────────────┐
│ Check Auth      │
│ (Clerk Session) │
└────┬────────────┘
     │
┌────▼────┐
│ Signed  │
│   In?   │
└────┬────┘
     │
  ┌──┴──┐
  │     │
 Yes   No
  │     │
  │     └──► Show Login Screen
  │
  ▼
┌──────────────────┐
│ Sync User to     │
│ Backend          │
│ POST /auth/sync  │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ Store            │
│ clerk_user_id    │
│ in AsyncStorage  │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ Fetch User's     │
│ Devices          │
│ GET /devices     │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ Show Dashboard   │
└──────────────────┘
```

---

## 🔒 Security Model

```
Request Security:

┌─────────────────────────────────────┐
│ Mobile App Request                  │
│                                     │
│ Headers:                            │
│   X-Clerk-User-Id: user_abc123      │
└──────────────┬──────────────────────┘
               │
               ▼
        ┌────────────┐
        │ Backend    │
        │ Validates  │
        └─────┬──────┘
              │
        ┌─────▼─────┐
        │ User      │
        │ Exists?   │
        └─────┬─────┘
              │
          ┌───▼───┐
          │       │
         Yes     No
          │       │
          │       └──► Return Empty/403
          │
          ▼
    ┌───────────┐
    │ Check     │
    │ Device    │
    │ Ownership │
    └─────┬─────┘
          │
      ┌───▼───┐
      │       │
    Owner  Not Owner
      │       │
      │       └──► Return 403 Forbidden
      │
      ▼
┌──────────┐
│ Return   │
│ Data     │
└──────────┘
```

---

## 📊 Data Flow Summary

```
1. User Authentication
   Clerk → Mobile App → Backend Sync → Database

2. Device Association
   User Creation → Device Creation → Link via user_id

3. Location Tracking
   GPS Hardware → TCP Server → Database → API → Mobile App

4. Access Control
   API Request → Header Check → User Lookup → Permission Check → Data Filter
```

---

This architecture provides:
- ✅ Clear separation of concerns
- ✅ Secure user data isolation
- ✅ Scalable multi-user support
- ✅ Simple integration with Clerk
- ✅ Backward compatible design
